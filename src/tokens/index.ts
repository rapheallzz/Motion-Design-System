export * from './motion';
export * from './visual';

import { motionTokens } from './motion';
import { visualTokens } from './visual';

export const tokens = {
  motion: motionTokens,
  visual: visualTokens,
};

export default tokens;
